# Deprecated folder

The API for this app lives in **`../server/`** (not here).

```bash
cd server
npm install
npm start
```

Default URL: `http://127.0.0.1:3001` — Flutter uses `/index.php?action=...` on that host.

You can delete `backend/node_modules` if it is still on disk (left over from an old setup).
